spring.application.name=psylog-api

# Server
server.port=${PORT:8080}

# Database
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/psylog}
spring.datasource.username=${DATABASE_USERNAME:YOUR_DB_USERNAME}
spring.datasource.password=${DATABASE_PASSWORD:YOUR_DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
spring.jpa.show-sql=${JPA_SHOW_SQL:true}
spring.jpa.properties.hibernate.format_sql=${JPA_SHOW_SQL:true}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT
jwt.secret=${JWT_SECRET:YOUR_JWT_SECRET}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Admin seed
admin.email=${ADMIN_EMAIL:YOUR_ADMIN_EMAIL}
admin.password=${ADMIN_PASSWORD:YOUR_ADMIN_PASSWORD}

# CORS — comma separated origin list
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000}

# Auth cookie — production: true / None / .yourdomain.com
cookie.secure=${COOKIE_SECURE:false}
cookie.same-site=${COOKIE_SAME_SITE:Lax}
cookie.domain=${COOKIE_DOMAIN:}

# Mail (Gmail SMTP) — MAIL_APP_PASSWORD must be a Gmail app password
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_APP_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Notification target email
app.notification.email=${NOTIFICATION_EMAIL:}

# Daily notification email cap — Gmail quota safety net
app.mail.daily-limit=${MAIL_DAILY_LIMIT:50}

# Rate limiting for the public endpoints that trigger notification emails (per IP)
app.rate-limit.contact.per-hour=${RATE_LIMIT_CONTACT_PER_HOUR:3}
app.rate-limit.contact.per-day=${RATE_LIMIT_CONTACT_PER_DAY:10}
app.rate-limit.comment.per-hour=${RATE_LIMIT_COMMENT_PER_HOUR:5}
app.rate-limit.comment.per-day=${RATE_LIMIT_COMMENT_PER_DAY:20}
